use sqlx::PgPool;

use crate::models::domain::Token;

// TODO: Implement token operations
// - create_token(pool: &PgPool, ticker: String, decimals: u8, name: String) -> Result<Token, sqlx::Error>
// - get_token(pool: &PgPool, ticker: &str) -> Result<Token, sqlx::Error>
// - list_tokens(pool: &PgPool) -> Result<Vec<Token>, sqlx::Error>
